console.log("maincore/app");

/* Memanipulasi data dari konfigurasi.json */
// function manip_config(data) {
//     let Base_Url = document.querySelectorAll('[data-web]');
//     Base_Url.forEach(el => {
//         el.outerHTML = re_outer(el.outerHTML, data);
//     });
// }

// load_json('konfigurasi.json', manip_config);